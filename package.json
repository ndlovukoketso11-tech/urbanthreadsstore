{
  "name": "urbanthreads-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "http-server -c-1 -p 8080",
    "test": "start-server-and-test start http://localhost:8080 jest --runInBand"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "jest": "^29.0.0",
    "puppeteer": "^20.11.0",
    "start-server-and-test": "^1.14.0"
  }
}